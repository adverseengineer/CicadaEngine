write AssetManager and use it to replace ManagedObject

make UBOs select their binding points dynamically, just select the first unused one

make materials collect a cache of all shader uniforms and update them every game loop
their values are set in the material

query GPU limits and capabilities at engine startup

move all applicable inline functions into their own source file

for any class that follows the singleton pattern, you can expose it to lua by pushing a global onto the stack that's just a call to the instance method